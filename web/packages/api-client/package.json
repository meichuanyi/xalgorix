{
  "name": "@xalgorix/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Generated TypeScript client for the Xalgorix Cloud_Platform REST API. Implements design.md `web/packages/api-client`; codegen target for `/api/v1/openapi.json`.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "test": "echo \"@xalgorix/api-client has no tests yet\" && exit 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "echo \"OpenAPI codegen wired in Phase 8 (task 8.2)\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@xalgorix/eslint-config": "workspace:*",
    "@xalgorix/tsconfig": "workspace:*",
    "typescript": "^5.5.4"
  }
}
